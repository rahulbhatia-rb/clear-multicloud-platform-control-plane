# Linkerd operating model
Monitor control-plane availability, proxy injection coverage, identity certificate lifecycle, proxy CPU/memory overhead, service success/latency and authorization policy drift. The mesh is itself a production dependency and needs SLOs/runbooks.
